#include <stdio.h>

/**
 * main - Entry point
 * Description: Lowercase
 * Return: Always 0 (success)
 */

int main(void)
{

int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	putchar(ch);
	putchar (‘\n’)
	getch();

}
